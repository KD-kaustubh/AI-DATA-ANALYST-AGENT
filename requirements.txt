# Installs the analyst package itself (editable) plus its dependencies,
# which are declared in pyproject.toml, and the test-only extras.
#
#   pip install -r requirements.txt
#
# Installing the package is what puts `analyst` on the import path, so
# `uvicorn analyst.api:app` works without setting PYTHONPATH.
-e .[dev]
